# ============================================
# LINEスタンプ丸投げちゃん 環境変数設定
# ============================================
# このファイルを .env にコピーして使用してください
# cp .env.example .env

# ============================================
# AI引き継ぎ情報
# ============================================
# プロジェクト: LINEスタンプ丸投げちゃん
# 主要機能: Gemini APIでLINEスタンプ画像を自動生成
# 技術スタック: Python, Flask, Gemini API
#
# 重要ルール:
# - 画像生成には gemini-3-pro-image-preview を使用
# - テキスト生成には gemini-3-pro-preview を使用
# - gemini-2.x, gemini-1.x, flash系は使用禁止
# - 直接プロンプトで画像生成せず、3ステップワークフローを使用

# ============================================
# セキュリティ注意事項（重要）
# ============================================
# 1. GEMINI_API_KEYは絶対にGitにコミットしない
# 2. APIキーをコードにハードコードしない
# 3. .gitignoreで以下を除外:
#    - .env, .env.local
#    - *_config.json
#    - .claude/
# 4. UIから入力されたAPIキーはsessionStorageに保存（サーバー保存しない）
# 5. Gemini APIキーが漏洩した場合は即座に再生成:
#    https://aistudio.google.com/app/apikey

# ============================================
# Gemini API設定
# ============================================
# Google AI StudioでAPIキーを取得（無料枠あり）
# https://aistudio.google.com/app/apikey
# 注意: UIからも設定可能。envに設定しなくてもOK

# GEMINI_API_KEY=your_api_key_here

# ============================================
# サーバー設定
# ============================================
PORT=5000
DEBUG=false
